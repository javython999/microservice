server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/user-db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

client:
  point-service:
    url: http://localhost:8082